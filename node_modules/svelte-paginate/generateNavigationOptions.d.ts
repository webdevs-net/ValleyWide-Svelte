import type { NavigationOption } from './types';
interface GenerateOptionsInput {
    totalItems: number;
    pageSize: number;
    currentPage: number;
    showStepOptions?: boolean;
    limit?: number;
}
export declare function generateNavigationOptions({ totalItems, pageSize, currentPage, limit, showStepOptions }: GenerateOptionsInput): NavigationOption[];
export {};
