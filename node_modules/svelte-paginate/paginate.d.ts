interface PaginateInput<T = unknown> {
    items: T[];
    pageSize: number;
    currentPage: number;
}
export declare function paginate<T>({ items, pageSize, currentPage }: PaginateInput<T>): T[];
export {};
